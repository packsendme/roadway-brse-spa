/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TwoDigitDecimaNumberDirective } from './TwoDigitDecimaNumber.directive';

describe('Directive: TwoDigitDecimaNumber', () => {
  it('should create an instance', () => {
    const directive = new TwoDigitDecimaNumberDirective();
    expect(directive).toBeTruthy();
  });
});
